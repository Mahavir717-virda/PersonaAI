# Docker

The root `docker-compose.yml` is the source of truth for local container orchestration. This directory is reserved for future Docker support files such as database initialization scripts, observability sidecars, or production deployment profiles.
